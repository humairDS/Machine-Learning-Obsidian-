==## Introduction Basic Questions==

1. What is the difference between a database and a table?
2. What is a row?
3. What is a column?
4. What does SQL stand for?
5. Why is SQL important for Data Science?
6. Which language communicates directly with databases?
7. Can one database contain multiple tables? Explain.


## Chapter 1: Questions
==*Part 1 Questions*==
1. What is the difference between **data**, a **table**, and a **database**?
2. Why do we use a **DBMS** instead of storing data directly in files?
3. What is the difference between a **row** and a **column**?
4. Why is SQL called a **query language**?
5. Why is SQL an important skill for a data scientist?

==*Part 2 Questions*==
6. Why is storing all information in one large table a bad idea?
7. What is a relational database?
8. What is the purpose of a **Primary Key**?
9. Can two rows have the same primary key? Why or why not?
10. What is a **Foreign Key**, and how does it help connect tables?
11. In the example above, which column is the foreign key in the `Students` table?
12. Why does using multiple related tables make updating data easier?